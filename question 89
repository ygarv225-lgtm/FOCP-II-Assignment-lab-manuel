#include<stdio.h>
#include<math.h>
#include<stdlib.h>
#include<limits.h>
int main(){
   // computing the minimum of the array
   int a=INT_MAX;
   printf("enter how many element you want in a array ");
   scanf("%d",&a);
   int arr[a];
   for(int i=0;i<a;i++){
    printf("enter the %d index element ",i);
    scanf("%d",&arr[i]);
    if(a>arr[i]) a=arr[i];    
   }
   printf("the minimun number is %d ",a);
    return 0;
}
